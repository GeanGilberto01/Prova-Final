Antes de tentar realizar qualquer operação via rotas "turmas" ou "alunos", 
deve-se antes realizar a execução do script contido no arquivo "banco.sql" 
dentro do banco de dados mysql.

Apos a execução do script, o banco sera criado e será possivel, realizar os 
procedimentos de CRUD dentro do banco via rotas de acesso.